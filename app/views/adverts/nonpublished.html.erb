<% if @adverts.empty? %>
    <h3>No nonbublished adverts.</h3>
<% else %>
    <h3>Nonpublished adverts:</h3>
    <%= button_to 'Approve all', approve_all_adverts_path, class: 'btn btn-danger btn-block' %>
    <%= render @adverts %>
    <%= paginate @adverts %>
<% end %>
