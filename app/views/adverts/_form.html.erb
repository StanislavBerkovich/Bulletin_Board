<%= simple_form_for(@advert) do |f| %>

    <div class="form-inputs">
      <%= f.input :body %>
    </div>


 <%= f.select :type, @types %>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>

<% end %>
