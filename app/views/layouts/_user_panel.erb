<div class="well sidebar-nav">
  <ul class="nav nav-list">
    <% if user_signed_in? %>
        <li><p>Hi, <%= full_name current_user %></p></li>
        <li>  <%= button_to 'Log out', destroy_user_session_path, :method => :delete, class: 'btn btn-warning btn-block' %></li>
    <% else %>
        <li><p>You are Guest</p></li>
        <li> <%= button_to 'Log in', new_user_session_path, class: 'btn btn-primary btn-block' %></li>
        <li> <%= button_to 'Registration', new_user_registration_path, class: 'btn btn-block' %></li>
    <% end %>

    <ul class="nav nav-header">Your Ability</ul>
    <% if can? :create, Advert.new %>
        <li><%= button_to "New advert", new_advert_path, method: :get, class: 'btn btn-success btn-block' %></li>
    <% end %>
    <% if can? :edit, current_user %>
        <li><%= button_to "Edit my profile", edit_user_registration_path(current_user), method: :get, class: 'btn btn-warning btn-block' %></li>
    <% end %>
  </ul>
</div>