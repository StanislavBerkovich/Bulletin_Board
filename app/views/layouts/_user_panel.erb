<div class="well sidebar-nav">
  <ul class="nav nav-list">
    <% if user_signed_in? %>
        <li><p>Hi, <%= full_name current_user %></p></li>
        <li>  <%= link_to 'Log out', destroy_user_session_path, :method => :delete, class: 'btn btn-warning' %></li>
    <% else %>
        <li><p>You are Guest</p></li>
        <li> <%= link_to 'Log in', new_user_session_path, class: 'btn btn-primary' %></li>
        <li> <%= link_to 'Registration', new_user_registration_path, class: 'btn' %></li>
    <% end %>

    <ul class="nav nav-header">Your Ability</ul>
    <% if can? :create, Advert.new %>
        <li><%= link_to "New advert", new_advert_path, method: :get, class: 'btn btn-success' %></li>
    <% end %>
    <% if can? :edit, current_user %>
        <li><%= link_to "Edit my profile", edit_user_registration_path(current_user), method: :get, class: 'btn btn-warning' %></li>
    <% end %>
  </ul>
</div>